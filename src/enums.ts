// === === === WWEB === === === //

export enum Commands {
  HELP = "help",
  TEST = "test",
  PING = "ping",
  REGISTER = "register"
}

export enum UserPositions {
  COMPANION = "companion",
  COLLABORATOR = "collaborator",
  ASSISTANT = "assistant"
}
