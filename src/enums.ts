// === === === WWEB === === === //

export enum Commands {
  HELP = "help",
  PING = "ping",
  REGISTER = "register"
}

export enum UserPositions {
  HAMRAH = "hamrah",
  HAMYAR = "hamyar",
  DASTYAR = "dastyar"
}
