- [ - ] `check in` - `check out` commands for whatsapp.
  save users and their positions in a set/map;

  - when `checking in`:
    - identify the messenger and their role.
    - get the date/time.
    - set `check-status` to `"in"`.
  - when `checking out`:
    - do the same as `checking in` but set `check-status` to `"out"`.

- [ - ] When users check in, they cant check in anymore until they check out.
